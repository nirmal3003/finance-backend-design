spring:
  application:
    name: Finance-Backend-Design

  datasource:
    url: jdbc:mysql://localhost:3306/finance_db?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080